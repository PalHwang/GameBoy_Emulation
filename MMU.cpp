#include "stdafx.h"
#include "MMU.h"

byte MMU::rb(addr address){ return address;};

byte MMU::rw(addr address){ return address;};

byte MMU::wb(addr address,byte val){ return address;};

byte MMU::ww(addr address,byte val){ return address;};

